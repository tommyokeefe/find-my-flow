/**
 * the firebase config object is not saved to git.
 * You should grab the values from your firebase console and construct it in './firebase.ts'
 */
import { firebase } from './firebase';

export const config = {
    firebase,
};